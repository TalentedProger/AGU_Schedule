# ScheduleBot - AGU Student Schedule Telegram Bot

Telegram-бот для доставки расписания студентам АГУ с административной веб-панелью.

## 🚀 Быстрый старт

См. [STARTUP.md](STARTUP.md) для полной инструкции по запуску.

```bash
# 1. Запустить бота (терминал 1)
.\.venv\Scripts\python.exe -m app.main

# 2. Запустить админ-панель (терминал 2)
.\.venv\Scripts\python.exe -m app.main admin

# 3. Открыть http://127.0.0.1:8000/admin/login
```

## Возможности

### Для студентов (Telegram Bot):
- ✅ Регистрация с выбором курса и направления
- ✅ Ежедневная доставка расписания в 08:00 МСК
- ✅ Опциональные напоминания за 5 минут до пары
- ✅ Управление настройками (пауза, смена направления)
- ✅ Возможность делиться ботом с друзьями

### Для администраторов (Web Panel):
- ✅ Дашборд со статистикой
- ✅ CRUD операции с парами/направлениями/слотами
- ✅ Назначение пар на несколько направлений
- ✅ Рассылка сообщений с фильтрами
- ✅ Просмотр логов доставки с пагинацией
- ✅ Экспорт логов в CSV

## Технологический стек

- **Python:** 3.11+
- **Telegram Bot:** aiogram 3.x
- **Web Framework:** FastAPI
- **Database:** SQLite + aiosqlite
- **Scheduler:** APScheduler
- **Server:** Uvicorn
- **Templates:** Jinja2

Полная документация: [Docs/TechStack.md](Docs/TechStack.md)

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd AGU_Schedule
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните переменные:

```bash
copy .env.example .env  # Windows
# или
cp .env.example .env    # Linux/macOS
```

Получите токен бота у [@BotFather](https://t.me/BotFather) и заполните `.env`:

```env
BOT_TOKEN=your_bot_token_here
ADMIN_TG_ID=your_telegram_id
ADMIN_PASSWORD=your_secure_password
SECRET_KEY=generate_with_python_secrets
```

### 5. Инициализация базы данных

```bash
python -m app.db.init_db
```

### 6. Запуск проекта

```bash
# Запуск Telegram бота (терминал 1)
python -m app.main

# Запуск админ-панели (терминал 2)
python -m app.main admin
```

Админ-панель: http://127.0.0.1:8000/admin/login

## Резервное копирование

```bash
# Создать бэкап
python scripts/backup_db.py create

# Показать список бэкапов
python scripts/backup_db.py list

# Восстановить из бэкапа
python scripts/backup_db.py restore backups/schedule_backup_YYYYMMDD_HHMMSS.db
```

## Структура проекта

```
AGU_Schedule/
├── app/                    # Основное приложение
│   ├── bot/               # Telegram bot логика
│   ├── web/               # Admin panel (FastAPI)
│   ├── scheduler/         # APScheduler jobs
│   ├── db/                # Database models & queries
│   └── utils/             # Shared utilities
├── Docs/                  # Документация
├── data/                  # SQLite database
├── logs/                  # Логи приложения
└── tests/                 # Тесты
```

Полная документация структуры: [Docs/project_structure.md](Docs/project_structure.md)

## Документация

- **[PRD.md](Docs/PRD.md)** - Требования к продукту
- **[AppMap.md](Docs/AppMap.md)** - Архитектура приложения
- **[TechStack.md](Docs/TechStack.md)** - Технический стек
- **[Implementation.md](Docs/Implementation.md)** - План реализации
- **[project_structure.md](Docs/project_structure.md)** - Структура проекта
- **[UI_UX_doc.md](Docs/UI_UX_doc.md)** - Дизайн-система
- **[Bug_tracking.md](Docs/Bug_tracking.md)** - Отслеживание ошибок

## Разработка

### Установка dev-зависимостей

```bash
pip install -r requirements-dev.txt
```

### Запуск тестов

```bash
pytest
```

### Форматирование кода

```bash
black app/
```

## Лицензия

© 2025 AGU ScheduleBot. Все права защищены.

## Контакты

Для вопросов и предложений обращайтесь к разработчикам проекта.
