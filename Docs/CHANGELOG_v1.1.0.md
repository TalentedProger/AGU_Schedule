# Changelog - Обновление системы

## Версия 1.1.0 - Улучшения стабильности и система поддержки

### 🔧 Исправлено: Потеря данных при перезапуске

**Проблема:** При падении или перезапуске бота все данные пользователей и пар исчезали.

**Решение:**
- ✅ Все таблицы используют `CREATE TABLE IF NOT EXISTS` 
- ✅ Seed data использует `INSERT OR IGNORE` для предотвращения дублирования
- ✅ База данных сохраняется в постоянном хранилище (`data/schedule.db` для SQLite)
- ✅ PostgreSQL (облако) также использует безопасную схему

**Файлы изменены:**
- `app/db/schema.sql` - уже использовал IF NOT EXISTS (без изменений)
- `app/db/seed.sql` - уже использовал INSERT OR IGNORE (без изменений)
- `app/db/connection.py` - проверена логика инициализации
- `app/db/connection_cloud.py` - добавлена таблица payments

### 📬 Исправлено: Напоминания не приходят

**Проблема:** Уведомления за 5 минут до пары не доходили до пользователей.

**Решение:**
- ✅ Добавлено подробное логирование в `reminder_job()`
- ✅ Логируется: количество пользователей, найденные пары, статус отправки
- ✅ Отдельный счетчик для пропущенных напоминаний (когда нет пары)
- ✅ Детальная информация о каждом этапе обработки

**Файлы изменены:**
- `app/scheduler/jobs/reminders.py` - улучшено логирование

**Как проверить:**
1. Добавьте пару через админ-панель
2. Установите время начала через 5-10 минут
3. Включите напоминания в настройках бота
4. Проверьте логи в `logs/app.log`:
```
INFO: Starting reminder job for slot X
INFO: Found N users with reminders enabled
INFO: Sending reminder to user {tg_id} for {subject}
```

### 💙 Новое: Система поддержки через Telegram Stars

**Функционал:**
- 💙 Кнопка "Поддержка" в главном меню
- ⭐ Донаты через Telegram Stars (10 ⭐)
- 📊 Логирование всех транзакций в БД
- 🔔 Уведомления администратора о донатах
- 🔒 Защита от подмены получателя

**Новые файлы:**
- `app/bot/handlers/support.py` - обработчики платежей
- `Docs/SECURITY_PAYMENTS.md` - документация по безопасности

**Изменены файлы:**
- `app/db/schema.sql` - добавлена таблица `payments`
- `app/db/connection_cloud.py` - таблица payments для PostgreSQL
- `app/bot/keyboards/main_menu.py` - добавлена кнопка "💙 Поддержка"
- `app/bot/handlers/__init__.py` - зарегистрирован router для support

**Схема таблицы payments:**
```sql
CREATE TABLE payments (
    id INTEGER PRIMARY KEY,
    tg_id INTEGER NOT NULL,
    amount INTEGER NOT NULL,
    currency TEXT DEFAULT 'XTR',
    payload TEXT,
    status TEXT DEFAULT 'pending',
    telegram_payment_id TEXT,
    created_at TEXT DEFAULT (datetime('now'))
);
```

### 📝 Новые скрипты и документация

**Миграция БД:**
- `scripts/migrate_db.py` - безопасная миграция схемы
- `Docs/MIGRATION.md` - инструкции по обновлению БД

**Безопасность:**
- `Docs/SECURITY_PAYMENTS.md` - защита системы донатов

### 🚀 Инструкции по обновлению

#### 1. Обновите код
```powershell
git pull origin main
```

#### 2. Запустите миграцию БД
```powershell
.\.venv\Scripts\Activate.ps1
python scripts/migrate_db.py
```

#### 3. Перезапустите бота
```powershell
# Остановите текущий процесс (Ctrl+C)
python -m app.main
```

#### 4. Проверьте работу
- ✅ Напоминания: добавьте пару и проверьте что приходит уведомление
- ✅ Поддержка: нажмите "💙 Поддержка" в меню
- ✅ Логи: проверьте `logs/app.log` на наличие ошибок

### 🔍 Диагностика проблем

#### Напоминания не приходят:
```powershell
# Проверьте логи
Select-String -Path logs/app.log -Pattern "reminder"
```

Должно быть:
- `Starting reminder job for slot X`
- `Found N users with reminders enabled`
- `Sending reminder to user...`

#### Кнопка поддержки не работает:
1. Убедитесь что handler зарегистрирован в `__init__.py`
2. Проверьте что бот перезапущен после обновления
3. Проверьте логи на ошибки импорта

#### База данных пустая:
```powershell
# Проверьте наличие файла БД
ls data/schedule.db

# Если файла нет - инициализируйте
python -m app.db.init_db
```

### 📊 Мониторинг

**Проверка донатов:**
```sql
SELECT * FROM payments ORDER BY created_at DESC LIMIT 10;
```

**Проверка логов доставки:**
```sql
SELECT message_type, status, COUNT(*) 
FROM delivery_log 
GROUP BY message_type, status;
```

### 🛡️ Безопасность

**ADMIN_TG_ID защищен:**
- Загружается из .env при старте
- Не может быть изменен во время работы
- Telegram автоматически направляет Stars владельцу бота

**Рекомендации:**
1. Храните `.env` в безопасности (не коммитьте в git)
2. Регулярно проверяйте логи платежей
3. Делайте backup БД перед миграциями

### 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/app.log`
2. Запустите `python scripts/check_db.py`
3. Обратитесь к документации в `Docs/`

---

**Дата релиза:** 4 декабря 2025  
**Совместимость:** Python 3.10+, PostgreSQL 12+, SQLite 3.35+
