# 🔥 КРИТИЧЕСКАЯ ПРОБЛЕМА: Несколько экземпляров бота

## ❌ Текущая ситуация

### Проблема №1: Conflict - terminated by other getUpdates request

**Ошибка:**
```
TelegramConflictError: Conflict: terminated by other getUpdates request; 
make sure that only one bot instance is running
```

**Причина:**
- На Render запущено **ДВА экземпляра бота одновременно**!
- Старый процесс не убит перед запуском нового
- Оба процесса пытаются получать updates от Telegram

### Проблема №2: SQLite вместо PostgreSQL

**Лог показывает:**
```
📦 Database: SQLite (local)
```

**Должно быть:**
```
📦 Database: PostgreSQL (cloud)
```

**Причина:**
- `DATABASE_URL` не подключается из render.yaml
- Возможно проблема с именем базы данных в конфигурации

### Проблема №3: Ошибки 405 все еще возникают

**Ошибка:**
```
starlette.exceptions.HTTPException: 405: Method Not Allowed
```

**Причина:**
- Exception handler добавлен, но HEAD запросы попадают в routing до exception handler

---

## ✅ РЕШЕНИЯ

### 1. Остановить все экземпляры бота на Render

**Через Render Dashboard:**
1. Откройте https://dashboard.render.com
2. Найдите сервис `agu-schedulebot`
3. **Suspend service** (приостановить)
4. Подождите 1 минуту
5. **Resume service** (возобновить)

Это убьет все процессы и запустит только один.

**Альтернатива - через Render CLI:**
```bash
# Остановить сервис
render services suspend agu-schedulebot

# Подождать 30 секунд
Start-Sleep -Seconds 30

# Запустить заново
render services resume agu-schedulebot
```

### 2. Проверить DATABASE_URL в Render Dashboard

**Шаги:**
1. Откройте https://dashboard.render.com
2. Перейдите в сервис `agu-schedulebot`
3. Откройте вкладку **Environment**
4. Найдите переменную `DATABASE_URL`
5. **Проверьте что она существует и начинается с `postgresql://`**

**Если DATABASE_URL пустая или отсутствует:**
1. Перейдите в раздел **PostgreSQL** (база данных)
2. Скопируйте **Internal Database URL**
3. Вставьте её в переменную окружения `DATABASE_URL` сервиса

**Формат должен быть:**
```
postgresql://user:password@host:port/database
```

### 3. Убедиться что render.yaml правильно настроен

**Проверьте в render.yaml:**
```yaml
envVars:
  - key: DATABASE_URL
    fromDatabase:
      name: agu-schedulebot-db  # ← Имя должно совпадать с databases
      property: connectionString
```

**И секция databases:**
```yaml
databases:
  - name: agu-schedulebot-db  # ← То же имя!
    plan: free
    databaseName: schedulebot
```

---

## 🚀 Пошаговая инструкция исправления

### Шаг 1: Остановить все боты (КРИТИЧНО!)

```bash
# В Render Dashboard:
1. Services → agu-schedulebot → Manual Deploy → Suspend
2. Подождать 60 секунд
3. Resume
```

### Шаг 2: Проверить DATABASE_URL

```bash
# В Render Dashboard:
1. Services → agu-schedulebot → Environment
2. Найти DATABASE_URL
3. Если пустая - добавить из PostgreSQL → Internal Connection String
```

### Шаг 3: Закоммитить исправления

```powershell
git add .
git commit -m "Fix: Exception handler for 405 errors + Database connection warnings"
git push origin main
```

### Шаг 4: Проверить логи после deploy

**Ожидаемый результат:**
```
📦 Database: PostgreSQL (cloud)  ← Должно быть PostgreSQL!
✅ Database already has data - SKIPPING initialization
Bot polling started...
✅ NO conflict errors!
```

**Если видите:**
```
📦 Database: SQLite (local)
⚠️  WARNING: Using SQLite in production!
⚠️  Set DATABASE_URL environment variable for PostgreSQL
```

→ **DATABASE_URL не подключена!** Вернитесь к Шагу 2.

---

## 🔍 Диагностика

### Проверка 1: Сколько ботов запущено?

**В логах Render ищите:**
```
Bot polling started...
```

Если эта строка появляется **больше 1 раза подряд** → запущено несколько ботов!

**Решение:** Suspend + Resume сервиса.

### Проверка 2: Используется ли PostgreSQL?

**В логах ищите:**
```
📦 Database: PostgreSQL (cloud)
```

Если видите `SQLite (local)` → DATABASE_URL не работает.

**Решение:** Проверить Environment Variables в Render Dashboard.

### Проверка 3: Есть ли ошибки 405?

**В логах ищите:**
```
Exception in ASGI application
405: Method Not Allowed
```

Если есть → exception handler не сработал.

**Решение:** Уже исправлено в коде, нужен redeploy.

---

## 📊 Мониторинг после исправления

### Через 5 минут после deploy проверьте:

**1. Логи должны быть чистыми:**
```bash
# НЕ должно быть:
✗ TelegramConflictError
✗ 405: Method Not Allowed  
✗ SQLite (local)

# Должно быть:
✓ PostgreSQL (cloud)
✓ Bot polling started... (ОДИН РАЗ!)
✓ Application startup complete
✓ No errors in logs
```

**2. Бот должен отвечать в Telegram:**
- Отправьте `/start` боту
- Должен ответить мгновенно
- Нет задержек или ошибок

**3. Админ-панель работает:**
- Откройте https://agu-schedule.onrender.com
- Должен перенаправить на /admin/login
- HEAD запросы не вызывают ошибок

---

## ⚠️ ВАЖНО: Проблема с несколькими ботами

### Почему возникает конфликт?

Telegram API позволяет **только один active getUpdates** на один токен. Если запущено два бота с одним токеном:

1. Первый бот: `getUpdates()` → 200 OK
2. Второй бот: `getUpdates()` → **409 Conflict**
3. Первый бот: следующий `getUpdates()` → **409 Conflict** (прерван вторым)
4. Оба бота постоянно конфликтуют

### Как это произошло?

**Render auto-deploy:**
1. Deploy начался
2. Новый контейнер запустился
3. **Старый контейнер еще не убит**
4. Оба контейнера запускают бота
5. **Conflict!**

### Решение:

**Временное (для текущего deploy):**
- Suspend → Resume сервиса

**Постоянное:**
- Render должен убивать старые процессы перед запуском новых
- Это стандартное поведение, проблема может быть в `restartPolicy`

---

## 📝 Чеклист после исправления

- [ ] Остановлены все экземпляры бота (Suspend + Resume)
- [ ] DATABASE_URL проверена и работает
- [ ] Код с исправлениями закоммичен и задеплоен
- [ ] В логах только один "Bot polling started..."
- [ ] Нет ошибок TelegramConflictError
- [ ] Используется PostgreSQL (не SQLite)
- [ ] Нет ошибок 405 в логах
- [ ] Бот отвечает в Telegram
- [ ] Админ-панель работает

---

**Дата:** 5 декабря 2025  
**Приоритет:** 🔥🔥🔥 КРИТИЧЕСКИЙ  
**Статус:** Требуется ручное вмешательство в Render Dashboard
