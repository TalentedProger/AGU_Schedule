# Database Migration & Updates

## После обновления кода - Важно!

После получения новой версии кода необходимо обновить схему базы данных.

### 1. Обновление локальной БД (SQLite)

```powershell
# Активируйте виртуальное окружение
.\.venv\Scripts\Activate.ps1

# Запустите миграцию
python scripts/migrate_db.py
```

Миграция безопасна - она использует `CREATE TABLE IF NOT EXISTS`, поэтому:
- ✅ Существующие данные НЕ будут удалены
- ✅ Новые таблицы будут добавлены
- ✅ Существующие таблицы останутся без изменений

### 2. Обновление облачной БД (PostgreSQL)

Если вы используете Railway/Render:

```powershell
# Установите переменную окружения DATABASE_URL
$env:DATABASE_URL = "your_postgres_connection_string"

# Запустите миграцию
python scripts/migrate_db.py
```

### Что было добавлено в этом обновлении:

#### 1. Таблица payments
- Хранит историю донатов через Telegram Stars
- Поля: tg_id, amount, currency, status, telegram_payment_id, created_at

#### 2. Улучшенная система напоминаний
- Добавлено подробное логирование
- Улучшена отладка проблем с доставкой уведомлений
- Счетчик пропущенных напоминаний (когда у пользователя нет пары)

#### 3. Система поддержки через Telegram Stars
- Кнопка "💙 Поддержка" в главном меню
- Обработка платежей через Telegram Stars (XTR)
- Безопасная проверка получателя платежа
- Логирование всех транзакций

### Проверка успешной миграции

После миграции проверьте:

```powershell
# Проверьте структуру БД
python scripts/check_db.py
```

Вы должны увидеть новую таблицу `payments` в списке.

### Возможные проблемы

**Ошибка: "table already exists"**
- Это нормально! Миграция пропустит уже существующие таблицы

**Ошибка: "no such table: payments"**  
- Запустите миграцию: `python scripts/migrate_db.py`

**Напоминания не приходят**
- Проверьте логи: `logs/app.log`
- Убедитесь что у пользователя включены напоминания в настройках
- Проверьте что у пользователя есть пары в расписании на сегодня

### Тестирование новых функций

#### Тест поддержки через Stars:
1. Запустите бота
2. Нажмите "💙 Поддержка" в главном меню
3. Проверьте что отображается инвойс на 10 ⭐

#### Тест напоминаний:
1. Добавьте пару в расписание через админ-панель
2. Установите время начала пары на 5-10 минут вперед
3. Включите напоминания в настройках бота
4. Дождитесь напоминания (за 5 минут до пары)
5. Проверьте логи: должно быть "Sending reminder to user..."

---

## Backup перед миграцией (опционально)

Если хотите создать backup перед миграцией:

```powershell
# Backup SQLite
python scripts/backup_db.py

# Backup PostgreSQL (если используете)
pg_dump $env:DATABASE_URL > backup.sql
```

Backup сохранится в папке `data/backups/`.
