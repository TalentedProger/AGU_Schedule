# 👨‍💼 AGU ScheduleBot - Руководство администратора

## Доступ к админ-панели

### URL админ-панели

Локально: `http://127.0.0.1:8000/admin`
Production: `https://admin.yourdomain.com/admin`

### Вход в систему

1. Откройте `/admin/login`
2. Введите пароль из `.env` (переменная `ADMIN_PASSWORD`)
3. Нажмите "Войти"

## 📊 Дашборд

Главная страница показывает:
- **Статистика пользователей** — общее количество, по курсам, по направлениям
- **Статистика пар** — количество пар на сегодня
- **Последние доставки** — история отправленных сообщений

## 📚 Управление направлениями

### Просмотр направлений

Меню: **Направления** или `/admin/directions`

### Добавление направления

1. Нажмите "➕ Добавить направление"
2. Введите название (например: "Прикладная математика")
3. Выберите курс (1-4)
4. Нажмите "Создать"

### Редактирование/Удаление

- ✏️ Редактировать — изменить название или курс
- 🗑️ Удалить — удалить направление (⚠️ удалит все связанные пары!)

## ⏰ Управление временными слотами

Меню: **Время пар** или `/admin/slots`

### Редактирование времени

1. Нажмите ✏️ напротив нужной пары
2. Измените время начала и/или окончания
3. Нажмите "Сохранить"

⚠️ **Важно:** Изменение времени влияет на все пары и напоминания!

## 📅 Управление расписанием

Меню: **Расписание** или `/admin/pairs`

### Фильтры

Используйте фильтры для быстрого поиска:
- По курсу
- По направлению  
- По дню недели

### Добавление пары

1. Нажмите "➕ Добавить пару"
2. Заполните форму:
   - **Название предмета** — начните вводить, появится автодополнение
   - **Преподаватель** — ФИО полностью
   - **Аудитория** — номер кабинета или "онлайн"
   - **Тип занятия** — лекция, практика, семинар и т.д.
   - **День недели** — когда проходит пара
   - **Время** — выберите временной слот
   - **Доп. ссылка** — ссылка на Zoom/Meet (необязательно)
3. **Выберите направления** — отметьте галочками группы
4. Нажмите "Создать пару"

### Редактирование пары

1. Найдите пару в списке
2. Нажмите ✏️
3. Внесите изменения
4. Нажмите "Сохранить"

### Удаление пары

1. Нажмите 🗑️ напротив пары
2. Подтвердите удаление

### Множественное назначение

Одна пара может быть назначена нескольким направлениям:
- Используйте кнопку "Выбрать все" для выбора всех групп
- Группы сгруппированы по курсам для удобства

## 📢 Рассылка сообщений

Меню: **Рассылка** или `/admin/broadcast`

### Создание рассылки

1. Выберите получателей:
   - Все пользователи
   - Конкретный курс
   - Конкретное направление
2. Введите текст сообщения
3. Используйте HTML-разметку для форматирования:
   ```html
   <b>жирный</b>
   <i>курсив</i>
   <code>код</code>
   <a href='url'>ссылка</a>
   ```
4. Проверьте предпросмотр справа
5. Нажмите "Отправить рассылку"
6. Подтвердите отправку

### Ограничения

- Максимум 4096 символов
- Рассылка идёт партиями по 30 сообщений
- Между партиями задержка 0.2 сек

## 📋 Просмотр логов

Меню: **Логи** или `/admin/logs`

### Фильтры логов

- **Тип сообщения** — morning, reminder, broadcast
- **Статус** — sent, error
- **Дата** — фильтр по дате

### Экспорт

Нажмите "📥 Экспорт CSV" для выгрузки логов в файл.

### Значения статусов

- ✅ `sent` — успешно доставлено
- ❌ `error` — ошибка доставки (пользователь заблокировал бота и т.д.)

## 🔧 Обслуживание

### Резервное копирование

```bash
# Создать бэкап
python scripts/backup_db.py create

# Список бэкапов
python scripts/backup_db.py list

# Восстановить из бэкапа
python scripts/backup_db.py restore backups/schedule_20251203_120000.db
```

### Перезапуск сервисов

```bash
# Перезапуск бота
sudo systemctl restart schedulebot

# Перезапуск админ-панели
sudo systemctl restart schedulebot-admin

# Оба сервиса
sudo systemctl restart schedulebot schedulebot-admin
```

### Просмотр логов

```bash
# Логи бота
sudo journalctl -u schedulebot -f

# Логи админ-панели
sudo journalctl -u schedulebot-admin -f

# Файловые логи
tail -f logs/app.log
```

## 🚨 Решение проблем

### Пользователи не получают сообщения

1. Проверьте статус бота: `systemctl status schedulebot`
2. Проверьте логи на ошибки
3. Убедитесь, что у пользователей есть пары на сегодня
4. Проверьте, что пользователи не на паузе

### Админ-панель недоступна

1. Проверьте статус: `systemctl status schedulebot-admin`
2. Проверьте порт: `netstat -tlnp | grep 8000`
3. Перезапустите сервис

### База данных заблокирована

```bash
# Перезапустите оба сервиса
sudo systemctl restart schedulebot schedulebot-admin
```

### Изменения не применяются

После обновления кода:
```bash
git pull
sudo systemctl restart schedulebot schedulebot-admin
```

## 📊 Мониторинг

### Ключевые метрики

- Количество активных пользователей
- Успешность доставки (процент ошибок)
- Время отклика админ-панели

### Автоматический мониторинг

Рекомендуется настроить:
- Уведомления при падении сервиса (systemd email notifications)
- Мониторинг дискового пространства (для логов и бэкапов)
- Регулярная проверка логов на ошибки

---

**Версия:** 1.0  
**Дата:** December 2025
