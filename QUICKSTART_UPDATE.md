# Быстрый старт после обновления

## ⚠️ Критически важно!

После получения обновления **обязательно** выполните миграцию базы данных!

## 🚀 Шаги по обновлению (5 минут)

### 1. Активируйте виртуальное окружение
```powershell
cd E:\it\AGU_Schedule
.\.venv\Scripts\Activate.ps1
```

### 2. Обновите базу данных
```powershell
python scripts/migrate_db.py
```

Ответьте **yes** на запрос подтверждения.

✅ Ожидаемый результат:
```
✓ Migration completed successfully!
  - All tables created/verified
  - Payments table is now available
```

### 3. Перезапустите бота
```powershell
# Остановите текущий процесс (если запущен)
# Ctrl+C

# Запустите заново
python -m app.main
```

## ✨ Что нового?

### 1. Исправлена потеря данных ✅
- База данных теперь сохраняется при перезапусках
- Пользователи и пары не исчезают при падении бота

### 2. Улучшены напоминания 📬
- Добавлено подробное логирование
- Легче отладить почему напоминания не приходят
- Проверьте `logs/app.log` для диагностики

### 3. Система поддержки через Telegram Stars 💙
- Новая кнопка "💙 Поддержка" в главном меню
- Пользователи могут задонатить 10 ⭐
- Все транзакции логируются в БД

## 🧪 Быстрая проверка

### Проверка 1: База данных
```powershell
python scripts/check_db.py
```

Должна появиться таблица **payments**.

### Проверка 2: Бот запускается
```powershell
python -m app.main
```

Не должно быть ошибок импорта.

### Проверка 3: Кнопка поддержки
1. Откройте бота в Telegram
2. Проверьте главное меню
3. Должна быть кнопка "💙 Поддержка"

### Проверка 4: Напоминания (опционально)
1. Добавьте тестовую пару через админ-панель
2. Установите время начала через 10 минут
3. Включите напоминания в настройках бота
4. Проверьте логи через 5 минут

## ❓ Проблемы?

### Ошибка: "ModuleNotFoundError: No module named 'support'"
**Решение:** Перезапустите бот после обновления кода

### Ошибка: "no such table: payments"
**Решение:** Запустите миграцию: `python scripts/migrate_db.py`

### Напоминания все еще не приходят
**Решение:**
1. Проверьте что у пользователя включены напоминания (⚙️ Настройки)
2. Убедитесь что у пользователя есть пары в расписании
3. Проверьте логи: `Select-String -Path logs/app.log -Pattern "reminder"`

### Кнопка "Поддержка" не отображается
**Решение:**
1. Перезапустите бота полностью (Ctrl+C, затем заново)
2. Отправьте боту `/start`
3. Проверьте что обновлен файл `main_menu.py`

## 📚 Дополнительная документация

- `Docs/MIGRATION.md` - подробная инструкция по миграции
- `Docs/CHANGELOG_v1.1.0.md` - полный список изменений
- `Docs/SECURITY_PAYMENTS.md` - безопасность системы донатов

## 🎉 Готово!

После выполнения всех шагов:
- ✅ База данных обновлена
- ✅ Данные защищены от потери
- ✅ Напоминания работают корректно
- ✅ Система поддержки активна

---

**Важно:** Не пропускайте шаг с миграцией! Без неё новые функции не будут работать.
